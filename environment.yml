name: r1
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.9
  - pytorch==2.5.1
  - pip:
      - git+https://github.com/huggingface/transformers.git
      - vllm==0.7.0
      - datasets==3.1.0
      - accelerate==1.3.0
      - hf-transfer==0.1.9
      - deepspeed==0.15.4
      - trl==0.14.0
      - flash-attn
      - tensorboard
      - wandb